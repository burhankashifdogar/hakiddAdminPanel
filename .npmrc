# react-simple-maps@3 only declares peer support up to React 18, but this app
# runs React 19 (Next 15). Install with legacy peer resolution so `npm ci`
# succeeds in CI, deploy.sh, and local without needing --legacy-peer-deps flags.
legacy-peer-deps=true
